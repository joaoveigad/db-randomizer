generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model decomposedNames {
  especime Int    @id @default(autoincrement())
  nome     String @unique
  prefixo  String
  fill     String
  sufixo   String
}
